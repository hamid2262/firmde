<h1>Listing kundens</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Url</th>
      <th>Photo</th>
      <th>Photo gray</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @kundens.each do |kunden| %>
      <tr>
        <td><%= kunden.name %></td>
        <td><%= kunden.url %></td>
        <td><%= image_tag kunden.photo.url %></td>
        <td><%= image_tag kunden.photo_gray.url %></td>
        <td><%= link_to 'Show', kunden %></td>
        <td><%= link_to 'Edit', edit_kunden_path(kunden) %></td>
        <td><%= link_to 'Destroy', kunden, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Kunden', new_kunden_path %>
