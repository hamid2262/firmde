<div id="page_show">
  <%= render 'pages/show/top_image' %>
  <%= render 'pages/show/breadcrumb' %>
  <% if can? :edit, @page %>
    <%= link_to 'Edit', edit_page_path(@page) %> |
    <%= link_to 'Back', pages_path %>
  <% end %>
  <div>
    <%= raw @page.body %>
  </div>
</div>
