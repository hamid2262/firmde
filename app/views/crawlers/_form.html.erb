<%= simple_form_for(@crawler) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :ip %>
    <%= f.input :head %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
